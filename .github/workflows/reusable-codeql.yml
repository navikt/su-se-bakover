name: CodeQL

on:
  workflow_call:

permissions: {}

jobs:
  codeql:
    permissions:
      contents: read
      security-events: write
    runs-on: ubuntu-latest
    name: CodeQL
    steps:
      - uses: actions/checkout@v3
      - name: CodeQL
        uses: ./.github/actions/codeql
