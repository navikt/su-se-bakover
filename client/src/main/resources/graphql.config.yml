schema: schema.graphql
documents: '**/*.graphqls'
include: ["./**"]
extensions:
  endpoints:
    One:
      url: https://saf.dev.intern.nav.no/graphql
      headers:
        Authorization: bearer TOKEN
